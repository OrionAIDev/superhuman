"""Portable session-fleet manifest package.

``scripts.fleet`` gives superhuman a concurrency-safe record of every session it
causes to exist — spawned, relayed, or manually handed off — plus the
dependencies between them. See ``docs/superhuman/session-tracking/DESIGN.md``.
"""
