# Leaky sample (fixture)

This file deliberately contains a planted synthetic token so the guard's
fixture test can prove detection fires. The next line uses a different case
from the token list on purpose, to prove matching is case-insensitive:

The nightly job deploys to VERMILLION7 after the smoke run.
