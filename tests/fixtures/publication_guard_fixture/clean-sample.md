# Clean sample (fixture)

This file contains none of the planted synthetic tokens. The guard's fixture
test asserts that scanning it yields no hits, so a mechanism that flagged
everything (or nothing) would be caught either way.
