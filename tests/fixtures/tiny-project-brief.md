# Tiny project: hello-world CLI

I want a Python CLI tool called `hello` that prints "Hello, <name>!" where `<name>` is given as a positional argument. If no name is given, it prints "Hello, world!". Include tests with pytest.
