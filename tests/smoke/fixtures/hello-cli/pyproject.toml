[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-cli"
version = "0.1.0"
description = "A tiny greeting CLI (superhuman smoke fixture)."
requires-python = ">=3.9"

[project.scripts]
hello-cli = "hello_cli:main"
